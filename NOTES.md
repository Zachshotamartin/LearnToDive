# Learn to Dive — v7 development rebuild

The current branch contains actual MuJoCo 3.13 articulated physics and an original closed-loop PPO actor. It is not frozen or release-qualified. The public manifest labels its snapshot development.

V6 fixed the v5 neutral-foot scoring error with a physical combined ankle/midfoot pitch range, unchanged 12 motors and torque limits, actual first-contact foot-line/hand geometry, continuous deductions and matching native/browser criteria. The 154,603,520-decision v5 optimizer checkpoint and complete native/WASM final-seed evidence remain preserved. The v6 phase is frozen at 164,249,600 lineage /9,646,080 v6 decisions. Its 64-start-per-skill development outcomes remain preserved and unqualified. The first v7 phase completed 20,480,000 decisions. A further 9,031,680-decision entry/ascent refinement is frozen with exact source before correcting delayed departure-velocity reward. The corrected departure-credit phase is preserved after 12,718,080 additional decisions. Current phase: `training/ppo-contact-exploration-v7`, branched from the strongest prior fixed-case entry checkpoint (191,303,680 lineage) with a fresh optimizer. Its training-only Gaussian variance has shape 6 × 2 × 9: increased hip/knee/ankle exploration only with observed loaded foot contact; narrow flight variance and deterministic actor unchanged at migration. The rationale and three-batch read-only gradient/noise audit are preserved in `training/diagnostics/v7-gradient-audit/`. V7 physics itself remains the same two added hip-adduction joints and 72-input/9-action controller. No old success is qualification evidence for v7.

Runtime API remains `mountExperiment(element,{embedded:true,assetBase})` with synchronous `dispose()`. `public/asset-manifest.json` includes immutable files plus matching physics/control/judge contracts. Models carry training source hashes and steps on the current contract. Export, native evaluation and browser startup reject legacy/mismatched contracts by default.

Current checks: 36 Node tests and 25 Python tests pass, including native/WASM 72-observation/14-joint trajectory parity, physical 100 ms foot correction and 200 ms leg closure, crossed-foot rejection, final-departure COM reset, bounded likelihood math, exact eight-action migration and contract mismatch rejection. Build and all 11 browser checks pass. The real WASM lifetime audit plateaus at 27.0 MB over 64 replays and 128 model/data recreations. Portable optimizer continuation passes from a temporary delivery-only directory. An actual 7,393,280-decision v7 rollout has a valid 7/10 103C entry with physically pointed feet and close legs. The rendered landmark audit is within 3.62e-8 m across frames; this is not qualification. The first frozen 384-case diagnostic is preserved in `training/diagnostics/v7-first-milestone/`, including all failures and a 96-case native/WASM comparison. The latter has identical validity, four half-point execution differences and up to 1.87° angle drift; final quality must pass independently in both runtimes.

Remaining: complete v7 training and development validation; evaluate frozen untouched final seed with all failures and per-skill foot/hand distributions; independent WASM qualification; final matching assets, model card, captures and browser/build review. No Git operations or publication are performed by this agent. Parent owns package files and publication; visual agent owns scene/art helpers.

Release export now reads each input snapshot once and refuses `--status release` without a matching frozen native/WASM qualification directory. Development export stays available; this guard does not claim the current controller is qualified.
